@model IEnumerable<FoodReport.DAL.Models.User>
@{
    ViewBag.Title = "List of users";
}


<a asp-action="Roles" class="btn btn-default">All roles</a>
<a asp-action="CreateUser" class="btn btn-default">Add user</a>

<table class="table">
    <tr><th>Email</th><th></th><th></th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post">
                    <a class="btn btn-default" asp-action="EditUser" asp-route-id="@user.Id">Edit user</a>
                    <a class="btn btn-default" asp-action="ChangePassword" asp-route-id="@user.Id">Change password</a>
                    <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                </form>
            </td>
        </tr>
    }
</table>